# 项目规则

- 地图绘制、标绘、几何展示和相关交互必须优先复用 `web/src/utils/cesium` 中已迁移的 Cesium 引擎能力。
- 仅当引擎明确缺少所需能力时，才允许在项目侧增加扩展；扩展的原因、边界和替代方案必须先记录到 OpenSpec 设计中。
- 影像像素坐标标注不属于当前 Cesium 引擎能力范围，可保留最小本地叠加层，并且仅用于构造投影 API 的像素输入。
