# 火点定位工作台：使用与启动说明

本目录包含火点定位工作台及示例数据。系统通过影像中的目标像素、相机位置/姿态、相机视场角和 DEM 地形数据，反算火点或标绘图形在 WGS84 坐标系中的地理位置。

## 目录说明

| 路径 | 用途 |
| --- | --- |
| `firepoint-geolocation-core` | 项目源码，包含 Python 后端、算法核心和 React 前端 |
| `导出历史报警数据_20260824153911` | 历史报警影像与表格示例数据 |
| `副本热成像摄像机列表(2).xlsx` | 摄像机信息示例表 |
| `openspec` | 项目需求、规格和变更记录 |

后续命令若未特别说明，均在 `D:\WebCode\calcfirepoint\firepoint-geolocation-core` 目录执行。

## 运行环境

- Windows 10/11，PowerShell。
- Python 3.11 或更高版本。
- Node.js 18 或更高版本，以及 pnpm（推荐使用与项目锁定文件匹配的 pnpm）。
- 可用的 WGS84 GeoTIFF 格式 DEM 文件。Web 工作台的定位接口依赖 DEM，缺失 DEM 时服务健康状态为 `degraded`，无法完成定位计算。

## 首次安装

### 1. 安装 Python 依赖

```powershell
cd D:\WebCode\calcfirepoint\firepoint-geolocation-core
python -m venv .venv
.\.venv\Scripts\Activate.ps1
python -m pip install --upgrade pip
python -m pip install -e ".[dev]"
```

若 PowerShell 阻止激活虚拟环境，可仅对当前窗口执行：

```powershell
Set-ExecutionPolicy -Scope Process -ExecutionPolicy Bypass
```

然后重新执行激活命令。

### 2. 安装前端依赖

另开一个 PowerShell 窗口：

```powershell
cd D:\WebCode\calcfirepoint\firepoint-geolocation-core\web
corepack enable
pnpm install --frozen-lockfile
```

如系统未启用 Corepack，请先安装 pnpm，或使用已安装的 pnpm 直接运行 `pnpm install --frozen-lockfile`。

## 配置 DEM

后端启动时会自动读取 `firepoint-geolocation-core\config.json`。该文件已创建并包含以下默认 DEM 路径：

```text
D:\GISData\Dem\重庆市\不统计\南岸区\重庆市_不统计_南岸区.tif
```

请直接编辑 `config.json` 中 `demSources.chongqing-nanan` 的 `path` 和 `verticalDatum`。路径请替换为实际 DEM 文件路径：

```json
{
  "demSources": {
    "chongqing-nanan": {
      "path": "D:\\GISData\\Dem\\你的DEM文件.tif",
      "verticalDatum": "ellipsoidal"
    }
  }
}
```

`verticalDatum` 必须填写 DEM 的真实高程基准，例如 `ellipsoidal` 或项目约定的其他值。相机绝对高程与 DEM 高程必须使用同一高程基准，否则定位结果不可信。

可选环境变量：

| 变量 | 说明 | 默认值 |
| --- | --- | --- |
| `VITE_API_BASE_URL` | 前端调用的后端 API 地址 | `http://127.0.0.1:8000` |
| `VITE_CESIUM_IMAGERY_URL` | Cesium 影像瓦片地址 | 项目内置的天地图服务地址 |
| `VITE_CESIUM_TERRAIN_URL` | Cesium 地形服务地址 | 项目内置的地形服务地址 |

前端变量应在 `web` 目录的启动命令前设置。例如：

```powershell
$env:VITE_API_BASE_URL = "http://127.0.0.1:8000"
```

## 启动工作台

### 1. 启动后端 API

在第一个 PowerShell 窗口执行：

```powershell
cd D:\WebCode\calcfirepoint\firepoint-geolocation-core
.\.venv\Scripts\Activate.ps1
$env:PYTHONPATH = "src"
uvicorn firepoint_api.main:app --host 0.0.0.0 --port 8000 --reload
```

服务默认监听 `http://127.0.0.1:8000`。启动后在浏览器访问以下地址确认 DEM 与 API 状态：

```text
http://127.0.0.1:8000/api/v1/health
```

返回 `"status": "available"` 且 DEM 源的 `available` 为 `true`，表示可以执行地形反算；若为 `degraded`，请检查 DEM 文件路径、文件访问权限和 GeoTIFF 格式。

### 2. 启动前端

在第二个 PowerShell 窗口执行：

```powershell
cd D:\WebCode\calcfirepoint\firepoint-geolocation-core\web
pnpm dev
```

打开命令行输出的本地地址，通常为：

```text
http://localhost:5173
```

按 `Ctrl+C` 可停止对应窗口中的服务。

## 页面使用

### 单点定位

1. 打开“单点定位”页；可上传一张报警或观测影像。系统会尝试读取影像元数据并自动回填可用参数。
2. 核对或填写相机经纬度、绝对高程、方位角、俯仰角、横滚角和水平/垂直视场角。
3. 在影像预览中确认目标框的像素坐标（`x`、`y`、`width`、`height`）。系统默认使用目标框中心作为反算像素。
4. 点击“计算定位”。计算成功后，三维地图与结果面板会展示目标的经纬度、地形高程、斜距和水平不确定度。

### 影像标绘

1. 切换到“影像标绘”页并选择影像。
2. 填写或确认左侧的相机与姿态参数。
3. 选择点、线或面标绘方式，在影像画布上绘制目标。
4. 执行投影后，系统会将各像素顶点投影到三维地图，并显示对应的坐标几何。

## 参数说明与注意事项

- 坐标系：相机经纬度和输出坐标均为 WGS84。
- 方位角：以真北为 `0°`，顺时针增加；代码可接受负数方位角。
- 俯仰角：水平向上为正，向下为负。要与地面相交，通常应为负值。
- 高程基准：相机绝对高程、DEM 高程必须一致；不能仅凭海拔名称假定其一致。
- DEM 覆盖范围：相机视线与目标位置必须落在 DEM 覆盖范围内。
- 目标选择：对地面火点，应标注火点的地面位置；若框选的是空中的烟羽，目标框中心未必代表落地点。
- 定位质量直接受相机位置、PTZ 姿态、标定视场角、像素标注和 DEM 精度影响。结果中的不确定度仅反映输入误差模型，不能替代现场复核。

详细的算法 JSON 输入字段见 [输入契约](firepoint-geolocation-core/docs/input-contract.md)。

## 命令行算法用法

无需启动 Web 服务，也可以直接执行算法核心：

```powershell
cd D:\WebCode\calcfirepoint\firepoint-geolocation-core
.\.venv\Scripts\Activate.ps1
$env:PYTHONPATH = "src"
python -m firepoint_core --input examples/dji_m4td_demo.json
```

若希望读取 DJI 影像的可用元数据：

```powershell
python -m firepoint_core --input examples/dji_m4td_demo.json --image "D:\路径\到\影像.jpg"
```

示例 JSON 使用的是平坦 DEM，仅用于验证计算流程，不代表真实火点定位结果。

## 验证与构建

后端测试：

```powershell
cd D:\WebCode\calcfirepoint\firepoint-geolocation-core
.\.venv\Scripts\Activate.ps1
$env:PYTHONPATH = "src"
python -m unittest discover -s tests -v
```

前端生产构建：

```powershell
cd D:\WebCode\calcfirepoint\firepoint-geolocation-core\web
pnpm build
```

## 常见问题

| 现象 | 排查方式 |
| --- | --- |
| 页面提示 API 服务或网络连接异常 | 确认后端仍在运行，并检查 `VITE_API_BASE_URL` 是否指向正确的 API 地址。 |
| 健康检查显示 `degraded` 或 `dem_source_unavailable` | 检查 `config.json` 中的 DEM 路径；确认目标 `.tif` 文件存在且可读取。 |
| 接口返回 `422` / `not_ready` | 检查相机位置、姿态、目标像素、DEM 覆盖范围和高程基准；俯仰角朝上时通常不会与地面相交。 |
| 三维地图没有影像或地形 | 检查网络能否访问配置的 Cesium 瓦片服务；必要时通过 `VITE_CESIUM_IMAGERY_URL` 和 `VITE_CESIUM_TERRAIN_URL` 替换为可用服务。 |
| 浏览器跨域报错 | 前后端非本机默认地址时，在 `config.json` 的 `cors.origins` 中加入前端地址后重启 API。 |
